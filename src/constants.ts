// ─── Timing constants ─────────────────────────────────────────────────────────
export const ISR_CACHE_SECONDS = 300;
export const SPLASH_DURATION_MS = 4200;
export const DOUBLE_TAP_THRESHOLD_MS = 300;
export const HOUR_POLL_INTERVAL_MS = 60_000;

// ─── Schedule image viewer ────────────────────────────────────────────────────
export const ZOOM_MIN = 1;
export const ZOOM_MAX = 5;

// ─── Shift active hours ───────────────────────────────────────────────────────
export const SHIFT_MORNING_START = 8;
export const SHIFT_MORNING_END = 11;
export const SHIFT_NIGHT_START = 22;

// ─── Dinner carousel visual tweaks ───────────────────────────────────────────
export const CAROUSEL_INACTIVE_SCALE = 0.91;
export const CAROUSEL_INACTIVE_OPACITY = 0.72;
